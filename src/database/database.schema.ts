import {
  accountsRelations,
  accountsTable,
} from 'src/accounts/entities/account.entity';
import {
  sessionsRelations,
  sessionsTable,
} from 'src/auth/entities/session.entity';
import { listsRelations, listsTable } from 'src/lists/entities/list.entity';
import { todosRelations, todosTable } from 'src/todos/entities/todo.entity';

export {
  accountsTable,
  accountsRelations,
  sessionsTable,
  sessionsRelations,
  listsTable,
  listsRelations,
  todosTable,
  todosRelations,
};
