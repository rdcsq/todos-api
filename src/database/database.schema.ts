import {
  accountsRelations,
  accountsTable,
} from 'src/accounts/entities/account.entity';
import {
  sessionsRelations,
  sessionsTable,
} from 'src/auth/entities/session.entity';

export { accountsTable, accountsRelations, sessionsTable, sessionsRelations };
